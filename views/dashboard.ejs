<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Dashboard</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 min-h-screen flex items-center justify-center">
  <div class="w-full max-w-2xl bg-white shadow-xl rounded-2xl p-8">
    <!-- Header -->
    <div class="flex justify-between items-center border-b pb-4">
      <h2 class="text-2xl font-bold text-gray-800">Welcome 🎉</h2>
      <a href="/logout" 
         class="bg-red-500 text-white px-4 py-2 rounded-lg hover:bg-red-600 transition duration-300">
         Logout
      </a>
    </div>

    <!-- User Info -->
    <div class="mt-6">
      <p class="text-lg text-gray-700">
        Logged in as: 
        <span class="font-semibold text-blue-600"><%= user.email %></span>
      </p>
    </div>

    <!-- Example Cards -->
    <div class="grid grid-cols-1 md:grid-cols-2 gap-4 mt-8">
      <div class="p-6 bg-blue-50 rounded-xl shadow">
        <h3 class="text-lg font-semibold text-blue-700">Profile</h3>
        <p class="text-sm text-gray-600 mt-2">Update your personal details and password.</p>
      </div>

      <div class="p-6 bg-green-50 rounded-xl shadow">
        <h3 class="text-lg font-semibold text-green-700">Settings</h3>
        <p class="text-sm text-gray-600 mt-2">Manage your account settings and preferences.</p>
      </div>
    </div>
  </div>
</body>
</html>
